eureka:
  client:
    service-url:
      defaultZone: http://localhost:9090/eureka/

server:
  port: 0
spring:
  application:
    name: java
  cloud:
    config:
      discovery:
        service-id: configserver
        enabled: true